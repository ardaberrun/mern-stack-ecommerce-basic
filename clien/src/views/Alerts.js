import {useMemo} from 'react'
import {useSelector} from 'react-redux'
import {message} from 'antd'

function Alerts() {
    // const state = useSelector((state) => state.message);

    // const messages = {
    //     success: (msg) =>  message.success(msg, 3),
    //     error: (msg) =>  message.error(msg, 3),
    // }

    // useMemo(() => {
    //     if(state.message) {
    //         messages[state.type](state.message)
    //     }
    // },[state])

    return (<></>)

    // return null
}

export default Alerts
